CREATE TABLE [dbo].[BDD_Class] (
    [ClassId]        INT          NOT NULL,
    [ClassName]      VARCHAR (50) NOT NULL,
    [ClassTypeID]    INT          NOT NULL,
    [CreationStatus] BIT          NOT NULL
);

